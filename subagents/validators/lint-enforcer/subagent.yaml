name: "lint-enforcer"
parent_agent: "qa-specialist"
task_scope: "code_style_validation"
max_execution_time: 30
max_tokens: 2000

inputs:
  - file_path
  - lint_tool (pint, phpcs)

outputs:
  - lint_report
  - auto_fix_applied
  - remaining_issues

validation:
  - lint_pass
  - no_style_violations
